# Oracle LiveLabs Author Guide

* `index.html` is the custom landing page.
* `assets/js/` holds the landing-page app, guide content data, and nav helper.
* `01-start-here` through `07-help-faq-support` hold the active guide source.
* `workshops/` holds the publishable LiveLabs variants.
* `archive-retired-reference/` keeps excluded or retired material.
* `project/` holds project-only output such as validation artifacts.
