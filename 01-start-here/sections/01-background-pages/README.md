Legacy holdover folder.

The active context pages were moved into `02-core-workshop-flow/sections/` so the file structure matches the guide structure.

The remaining files here are old image copies that are no longer part of the active reading path.
