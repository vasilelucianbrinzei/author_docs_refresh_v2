# Background Pages Holdover

This folder remains only as a historical holdover for old image copies.

The active WMS and GitHub context labs now live under `02-core-workshop-flow/sections/` so the tracked content matches the current guide flow.

Do not add new active guide content here.
